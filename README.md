Just a dummy project to test zc.buildout.

Replace cutecatlib in external-sources with your own private repo.

To get new buildout update, run:-

    git subtree pull --prefix vendor/buildout buildout use-pip --squash
